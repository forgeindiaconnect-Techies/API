# ─── Core Framework ────────────────────────────────────────────────────────────
setuptools
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.9
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.5.0
python-dotenv==1.0.0
aiofiles==23.2.1
sse-starlette==1.8.2

# ─── Auth & Security ──────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ─── Database ─────────────────────────────────────────────────────────────────
motor==3.3.2
pymongo==4.6.1

# ─── HTTP & Networking ────────────────────────────────────────────────────────
httpx>=0.27.0,<1.0.0
websockets==12.0
slowapi==0.1.9

# ─── Background Workers ──────────────────────────────────────────────────────
celery==5.3.6
redis==5.0.1

# ─── Hugging Face Ecosystem (pinned for mutual compatibility) ─────────────────
# IMPORTANT: huggingface-hub must NOT use [inference] extra (removed in recent versions).
# Pin this FIRST so pip does not backtrack.
huggingface-hub>=0.23.0,<0.25.0
tokenizers>=0.19.0,<0.21.0
transformers>=4.40.0,<4.46.0
sentence-transformers>=2.7.0,<3.0.0
torch>=2.1.2

# ─── RAG / Vector DB ─────────────────────────────────────────────────────────
# Only RecursiveCharacterTextSplitter is used — the lightweight text-splitters
# package replaces the full langchain/langchain-community/langchain-core stack.
langchain-text-splitters>=0.2.0,<0.4.0
chromadb>=0.5.0,<0.6.0

# ─── LLM Training ────────────────────────────────────────────────────────────
datasets>=2.16.0,<3.0.0
peft>=0.7.0,<1.0.0
trl>=0.7.0,<1.0.0
accelerate>=0.26.0,<1.0.0

# ─── Data Processing ─────────────────────────────────────────────────────────
pandas==2.2.0
numpy>=1.26.0,<2.0.0
scikit-learn==1.4.0
matplotlib==3.8.2
seaborn==0.13.2
openpyxl==3.1.2
PyPDF2==3.0.1
python-docx==1.1.0
Pillow>=10.2.0,<11.0.0

# ─── Cloud & AI Services ─────────────────────────────────────────────────────
boto3==1.34.27
openai==1.12.0
ollama>=0.6.2
cloudinary

# ─── Misc ─────────────────────────────────────────────────────────────────────
email-validator
types-requests